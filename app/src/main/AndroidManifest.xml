<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proffy">

        <activity
            android:name=".ui.FinallyActivity"
            android:exported="false" />
        <activity
            android:name=".ui.teacherList.TeacherListActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.register.RegisterStepTwoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.register.RegisterStepOneActivity"
            android:exported="false" />
        <activity
            android:name=".ui.forgot.ForgotActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>